package java.org.example;

import avm.Address;
//import org.aion.*;
import org.aion.avm.userlib.AionMap;

public class Aiomaps
{


    //<access-specifier> static AionList<datatype> <variableName> = new AionList<>();
    //private static AionList<String> carList = new AionList<>();
    //private static AionList<String> carListCopy = new AionList<>();

    //private static final AionMap <Integer,> Phonebook = new AionMap<>();

     Address addr;
     Float token;

    private static final AionMap<Integer,Aiomaps > mapctract  = new AionMap<>();
    //private static
    //public static void assign (Float t)
    {


        //<aionListObject>.add(variable);
        //carList.add(newCarBrand);
        //<aionListObject>.remove(variable);
        //carList.clear(); - REMOVE ALL ELEMENTS




       //check element exist in list -  return carList.contains(carBrand);
        //check list is empty = return carListCopy.isEmpty() ? true: false;


    }

    public static void removeaddr()
    {

    }
}